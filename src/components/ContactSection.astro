---
// importacion de iconos

// Informacion de contacto

type schemaContact = {
  email: string;
  phone: string;
  location: string;
};
const contactInfo: schemaContact = {
  email: "josuemarchena0730@gmail.com",
  phone: "+57 304 449 0607",
  location: "Colombia (Remote work)",
};
---

<section id="contact" class="py-16 px-4 bg-background mx-auto">
  <div class="container mx-auto max-w-4xl">
    <div class="text-center mb-12">
      <h2 class="text-3xl md:text-4xl font-bold mb-4">Let's Work Together!</h2>
      <p class="text-lg text-muted-foreground max-w-2xl mx-auto">
        I would love to be part of your team. I'm ready, motivated, and
        passionate about contributing to new goals and helping your projects
        reach the next level.
      </p>
    </div>

    <div
      class="flex flex-col justify-center items-center md:flex-row gap-8 mx-auto"
    >
      <!-- Columna de Información de Contacto -->
      <div class="space-y-6 mx-auto">
        <div class="bg-card border-border border rounded-lg mx-auto">
          <div class="p-6">
            <h3 class="text-xl font-semibold">Contact Information</h3>
            <p class="text-sm text-muted-foreground">
              Feel free to reach out through any of these channels.
            </p>
          </div>
          <div class="p-6 pt-0 space-y-4">
            <div class="flex items-center gap-3">
              <div class="p-2 bg-primary/10 rounded-lg">
                <!-- Mail Icon -->
                <svg
                  xmlns="http://www.w3.org/2000/svg"
                  width="24"
                  height="24"
                  viewBox="0 0 24 24"
                  fill="none"
                  stroke="currentColor"
                  stroke-width="2"
                  stroke-linecap="round"
                  stroke-linejoin="round"
                  class="h-4 w-4 text-primary"
                  ><rect width="20" height="16" x="2" y="4" rx="2"></rect><path
                    d="m22 7-8.97 5.7a1.94 1.94 0 0 1-2.06 0L2 7"></path></svg
                >
              </div>
              <div>
                <p class="font-medium">Email</p>
                <a
                  href={`mailto:${contactInfo.email}`}
                  class="text-sm text-muted-foreground hover:text-primary transition-colors"
                  >{contactInfo.email}</a
                >
              </div>
            </div>
            <div class="flex items-center gap-3">
              <div class="p-2 bg-primary/10 rounded-lg">
                <!-- Phone Icon -->
                <svg
                  xmlns="http://www.w3.org/2000/svg"
                  width="24"
                  height="24"
                  viewBox="0 0 24 24"
                  fill="none"
                  stroke="currentColor"
                  stroke-width="2"
                  stroke-linecap="round"
                  stroke-linejoin="round"
                  class="h-4 w-4 text-primary"
                  ><path
                    d="M22 16.92v3a2 2 0 0 1-2.18 2 19.79 19.79 0 0 1-8.63-3.07 19.5 19.5 0 0 1-6-6 19.79 19.79 0 0 1-3.07-8.67A2 2 0 0 1 4.11 2h3a2 2 0 0 1 2 1.72 12.84 12.84 0 0 0 .7 2.81 2 2 0 0 1-.45 2.11L8.09 9.91a16 16 0 0 0 6 6l1.27-1.27a2 2 0 0 1 2.11-.45 12.84 12.84 0 0 0 2.81.7A2 2 0 0 1 22 16.92z"
                  ></path></svg
                >
              </div>
              <div>
                <p class="font-medium">Phone</p>
                <a
                  href={`tel:${contactInfo.phone.replace(/\s/g, "")}`}
                  class="text-sm text-muted-foreground hover:text-primary transition-colors"
                  >{contactInfo.phone}</a
                >
              </div>
            </div>
            <div class="flex items-center gap-3">
              <div class="p-2 bg-primary/10 rounded-lg">
                <!-- MapPin Icon -->
                <svg
                  width="24"
                  height="24"
                  viewBox="0 0 24 24"
                  fill="none"
                  stroke="currentColor"
                  stroke-width="2"
                  stroke-linecap="round"
                  stroke-linejoin="round"
                  class="h-4 w-4 text-primary"
                  ><path d="M20 10c0 6-8 12-8 12s-8-6-8-12a8 8 0 0 1 16 0Z"
                  ></path><circle cx="12" cy="10" r="3"></circle></svg
                >
              </div>
              <div>
                <p class="font-medium">Location</p>
                <p class="text-sm text-muted-foreground">
                  {contactInfo.location}
                </p>
              </div>
            </div>
          </div>
        </div>
      </div>

      <!-- Columna del Formulario de Contacto -->
      <!-- <div class="bg-card border-border border rounded-lg">
        <div class="p-6">
          <h3 class="text-xl font-semibold">Envíame un Mensaje</h3>
          <p class="text-sm text-muted-foreground">
            Completa el formulario y te responderé lo antes posible.
          </p>
        </div>
        <div class="p-6 pt-0">
          <form id="contact-form" class="space-y-4">
            <div>
              <input
                name="name"
                placeholder="Tu nombre"
                required
                class="flex h-10 w-full rounded-md border border-input bg-background px-3 py-2 text-sm ring-offset-background file:border-0 file:bg-transparent file:text-sm file:font-medium placeholder:text-muted-foreground focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2 disabled:cursor-not-allowed disabled:opacity-50"
              />
            </div>
            <div>
              <input
                name="email"
                type="email"
                placeholder="Tu email"
                required
                class="flex h-10 w-full rounded-md border border-input bg-background px-3 py-2 text-sm ring-offset-background file:border-0 file:bg-transparent file:text-sm file:font-medium placeholder:text-muted-foreground focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2 disabled:cursor-not-allowed disabled:opacity-50"
              />
            </div>
            <div>
              <textarea
                name="message"
                placeholder="Cuéntame sobre tu proyecto..."
                rows="4"
                required
                class="flex min-h-[80px] w-full rounded-md border border-input bg-background px-3 py-2 text-sm ring-offset-background placeholder:text-muted-foreground focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2 disabled:cursor-not-allowed disabled:opacity-50"
              ></textarea>
            </div>
            <button
              type="submit"
              class="inline-flex items-center justify-center whitespace-nowrap rounded-md text-sm font-medium ring-offset-background transition-colors focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2 disabled:pointer-events-none disabled:opacity-50 bg-primary text-primary-foreground hover:bg-primary/90 h-10 px-4 py-2 w-full"
            >
              <!-- Send Icon -->
      <!-- <svg
        xmlns="http://www.w3.org/2000/svg"
        width="24"
        height="24"
        viewBox="0 0 24 24"
        fill="none"
        stroke="currentColor"
        stroke-width="2"
        stroke-linecap="round"
        stroke-linejoin="round"
        class="mr-2 h-4 w-4"
        ><path d="m22 2-7 20-4-9-9-4Z"></path><path d="M22 2 11 13"></path></svg
      >
      Enviar Mensaje
    </div> -->
    </div>
  </div>

  <!-- <script>
    document.addEventListener("DOMContentLoaded", () => {
      const form = document.getElementById("contact-form");
      if (form) {
        form.addEventListener("submit", (e) => {
          e.preventDefault();

          // Esto para obtener los datos del formulario fácilmente.
          const formData = new FormData(form as HTMLFormElement);
          const data = Object.fromEntries(formData.entries());

          console.log("Formulario enviado:", data);

          // Por ejemplo, podrías hacer un fetch a un endpoint.

          alert("Gracias por tu mensaje. ¡Te responderé pronto!");
          (form as HTMLFormElement).reset(); // Limpia el formulario después de enviarlo.
        });
      }
    });
  </script> -->
</section>
